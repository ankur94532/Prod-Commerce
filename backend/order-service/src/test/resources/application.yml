security:
  jwt:
    secret: test_secret_very_long_123456789012345
spring:
  datasource:
    url: jdbc:h2:mem:order_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
services:
  catalog:
    base-url: http://localhost:8082
