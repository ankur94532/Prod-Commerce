server:
  port: 8083

spring:
  application:
    name: cart-service

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

management:
  endpoints:
    web:
      exposure:
        include: health,info
